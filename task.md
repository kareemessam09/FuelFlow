You are a Senior Flutter Developer and Backend Integration Specialist.Task: Implement the critical fixes identified in the FuelFlow Sync Integrity Report. The goal is to achieve "Zero Logic Drift" between the Flutter app and the NestJS backend.Please apply the following specific code changes:1. Fix Time Drift in FuelBloc (_onSyncWithServer):After a successful server sync, ensure _lastDecayReferenceTime is reset to DateTime.now().This prevents the next tick from calculating decay based on an old reference point, which currently causes a volume "jump."2. Implement App Resumption Sync (_onResumeDecay):Modify the resume handler in FuelBloc. After applying the "offline decay" locally for instant UI feedback, you must trigger a FuelSyncWithServer event to reconcile the local state with the server's authoritative data.3. Fix Alert Notification Flow:Repository: Update FuelRepository.updateActivityModeWithAlert to return the DateTime? alertTime from the backend's POST /api/activity/toggle response.BLoC: In _onChangeActivity, catch this alertTime and pass it to NotificationService().scheduleCriticalAlert(alertTime) to ensure the background notification is scheduled based on the server's precise prediction.4. Logic Validation:Ensure the decay formula remains consistent:$$V_{remaining} = V_{start} - (R_{base} \times G_{index} \times M_{activity} \times \Delta t)$$.Verify that the currentGlycemicIndex in Flutter is always overwritten by the server's effectiveGlycemicIndex during every sync.Quality Check: Ensure all state changes are persisted to LocalStorageService and that the UI reflects the new "Sync Status" (isSyncing flag). Provide the full updated code for fuel_bloc.dart and fuel_repository.dart.
